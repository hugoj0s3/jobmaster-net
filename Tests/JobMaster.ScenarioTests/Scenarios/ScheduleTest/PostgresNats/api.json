{
  "containerName": "api",
  "dockerfilePath": "Tests/TargetTestApi/Dockerfile",
  "networkAlias": "api",
  "httpPort": 8080,
  "healthCheckPath": "/health",
  "auth": {
    "requireAuthentication": false
  },
  "clusterConfigTemplates": [
    {
      "ClusterId": "postgres-nats-dist-one",
      "SetAsDefault": true,
      "RepoType": "Postgres",
      "ConnectionString": "Host={{PostgresHost}};Port={{PostgresPort}};Database=PostgresNatsDistCluster;Username={{PostgresUsername}};Password={{PostgresPassword}}"
    },
    {
      "ClusterId": "postgres-nats-dist-two",
      "SetAsDefault": false,
      "RepoType": "Postgres",
      "ConnectionString": "Host={{PostgresHost}};Port={{PostgresPort}};Database=PostgresNatsDistCluster;Username={{PostgresUsername}};Password={{PostgresPassword}}"
    }
  ]
}
