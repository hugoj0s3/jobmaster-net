{
  "scenarioName": "mysql-pure",
  "phases": [
    { "name": "phase-1", "containers": ["mysql-standalone", "mysql-dist-one", "mysql-dist-two"] },
    { "name": "phase-2", "containers": ["mysql-standalone", "mysql-dist-one", "mysql-dist-two"] },
    { "name": "phase-3", "containers": ["mysql-dist-one"] }
  ],
  "api": { "container": "api" }
}
